package org.firstinspires.ftc.teamcode.util;

import androidx.annotation.Nullable;

import com.arcrobotics.ftclib.command.SubsystemBase;
import com.qualcomm.robotcore.hardware.HardwareMap;

public class RobotSubsystem extends SubsystemBase {

    /**
     * <p>Creates a new instance of the subsystem with default hardware initializations.</p>
     * <p>Returns null if no default creation procedure exists.</p>
     *
     * @param hardwareMap The hardware map to load devices from
     * @return A new instance of the subsystem, initialized with its default hardware
     */
    @Nullable
    public static RobotSubsystem createWithDefaults(final HardwareMap hardwareMap) {
        return null;
    }
}
